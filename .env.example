# Postgres
POSTGRES_USER=luc
POSTGRES_PASSWORD=luc
POSTGRES_DB=luc

# Keycloak
KEYCLOAK_ADMIN_CLIENT_SECRET=dev-secret
KEYCLOAK_LINKEDIN_CLIENT_ID=
KEYCLOAK_LINKEDIN_CLIENT_SECRET=

# SMTP — used by Keycloak (forgot password) AND the backend (sign-up codes).
# Omit / leave KEYCLOAK_SMTP_HOST unset for Mailhog.
# Mailgun (real inboxes):
#   US: smtp.mailgun.org
#   EU: smtp.eu.mailgun.org
# Get login+password from Mailgun → Sending → Domain → SMTP credentials.
# Sandbox domains can only send to authorized recipients.
KEYCLOAK_SMTP_HOST=smtp.mailgun.org
KEYCLOAK_SMTP_PORT=587
KEYCLOAK_SMTP_FROM=LUC <noreply@YOUR-DOMAIN.mailgun.org>
KEYCLOAK_SMTP_SSL=false
KEYCLOAK_SMTP_STARTTLS=true
KEYCLOAK_SMTP_AUTH=true
KEYCLOAK_SMTP_USER=postmaster@YOUR-DOMAIN.mailgun.org
KEYCLOAK_SMTP_PASSWORD=


# Frontend (next-auth)
NEXTAUTH_SECRET=replace-with-openssl-rand-base64-32

# Profile photos (optional). Docker Compose already sets this to /var/luc/uploads.
# LUC_UPLOADS_DIR=/var/luc/uploads
